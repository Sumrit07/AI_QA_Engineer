introduction to deep learning

ml ---> small dataset, dl ---> works on complex dataset
dl ----> artificial neural networks

#workflow od deeplearning

1 = data ingestion ---> data cleaning and preprocessing --> model sequential architexzture(
    input_layer(neurons,activation_fun,input_dimension)
    hidden_layer(neurons,activation_fun),
    output_layer(neurons_target_data,activation_target_data)
)

model.compile(loss,optimizer,metrics)
model.training
model.prediction
performance evlution

loss_fun ---> y_test-y_pred

activation fun ---> these are mathamatical function which is used ton intoduce non-linear from the data
when our data is numerical --->mostly used ---> ReLU(rectified Liner unit)
when our data is categorical ---> mostly use--->
1). Sigmoid(when we have 2 sub-categories in target column),
2). Softmax(when we have more than 2 sub-categories in taret column)

optimizer---> loss function to reduce 